<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.mapper.TestMapper">
    <insert id="saveA"
            parameterType="org.example.po.A">
        insert into a
        (id,msg)
        values(#{id},#{msg},#)

    </insert>
    <update id="update">

    </update>

</mapper>